PORT=5001
MONGO_URI=mongodb+srv://portfolio_user:portfolio_pass_2026@cluster0.mongodb.net/portfolio_db?retryWrites=true&w=majority

# CLIENT_URL = the FRONTEND URL allowed to call this backend (for CORS)
# This must be the frontend URL, NOT the backend's own URL
CLIENT_URL=http://localhost:3000

NODE_ENV=development
